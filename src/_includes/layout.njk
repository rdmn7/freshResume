<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ site.title }}</title>
    <link rel="stylesheet" href="/assets/css/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js" integrity="sha512-GsLlZN/3F2ErC5ifS5QtgpiJtWd43JWSuIgh7mbzZ8zBps+dvLusV+eNQATqgA/HdeKFVgA5v3S/cIrLF7QnIg==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    
    <style>
        :root {
            --color-primary: {{ site.colorScheme.primary }};
            --color-secondary: {{ site.colorScheme.secondary }};
            --color-accent: {{ site.colorScheme.accent }};
        }
    </style>
</head>
<body 
    data-animations-enabled="{{ site.animations.enabled | default(true) }}"
    data-text-scramble-enabled="{{ site.animations.textScramble | default(true) }}"
    data-scramble-phrases="{{ scramble.phrases | dump | safe }}"
    data-scramble-delay="{{ scramble.delay | default(2000) }}"
>
    <div class="theme-toggle-wrapper">
        <button id="contact-btn" class="contact-btn" aria-label="Contact me">
            <i class="fas fa-envelope"></i>
        </button>
        <button id="theme-toggle" class="theme-toggle" aria-label="Toggle dark mode">
            <i class="fas fa-moon"></i>
            <i class="fas fa-sun"></i>
        </button>
    </div>
    
    {% include "partials/contact-form.njk" %}

    {{ content | safe }}

    {% include "partials/footer.njk" %}

    <script src="/assets/js/text-scramble.js"></script>
    <script src="/assets/js/script.js"></script>
</body>
</html> 